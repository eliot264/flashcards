<UserControl x:Class="Flashcards.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Flashcards.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Style="{StaticResource LogoLabel}" Content="Flashcards"/>
            <Button x:Name="setButton" Style="{StaticResource NormalButton}" Width="200" Content="Sets" Command="{Binding GoToSetListingViewCommand}"/>
            <Button x:Name="exitButton" Style="{StaticResource NormalButton}" Width="200" Content="Exit" Command="{Binding ExitCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
